(()=>{'use strict';if(window.TDBCookieText350)return;const SELECTOR='style[data-tdb-consent-state]',FROM='animation:tdb-consent-text-in 520ms cubic-bezier(.5,0,1,1) 70ms both',TO='animation:tdb-consent-text-in 350ms cubic-bezier(.5,0,1,1) 70ms both';function patch(style){if(!(style instanceof HTMLStyleElement)||!style.matches(SELECTOR)||!style.textContent.includes(FROM))return false;style.textContent=style.textContent.replace(FROM,TO);return true}const existing=document.querySelector(SELECTOR);if(existing)patch(existing);else{const observer=new MutationObserver(mutations=>{for(const mutation of mutations){for(const node of mutation.addedNodes){if(node instanceof HTMLStyleElement&&patch(node)){observer.disconnect();return}}}});observer.observe(document.head||document.documentElement,{childList:true,subtree:true})}window.TDBCookieText350=Object.freeze({version:'1.0.0'})})();